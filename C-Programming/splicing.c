// C program to illustrate the concept of Line splicing. 
/* While writing a comment in a program if we use \(backslash) character at the end of this comment line , then the line just after the comment line will be ignored*/
#include <stdio.h> 
int main() 
{ 
    // Line Splicing\
    printf("Hello GFG\n"); 
    printf("welcome"); 
    return (0); 
} 

// O/P: welcome
